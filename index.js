/**
 * This is a fallback file for Render to detect the project type.
 * It manually triggers the backend server.
 */
console.log("Starting backend from root fallback...");
require('./backend/server.js');
